<template>
  <div>
    <h1>Welcome</h1>
    <p>A full-stack application with a <a href="https://nuxt.com/">Nuxt</a> frontend and an <a href="https://get.asp.net/">ASP.NET Core</a> backend, built with:</p>
    <ul>
      <li><a href="https://get.asp.net/">ASP.NET Core</a> and <a href="https://learn.microsoft.com/en-us/dotnet/csharp/">C#</a> for cross-platform server-side code</li>
      <li><a href="https://nuxt.com/">Nuxt</a> and <a href="https://vuejs.org/">Vue 3</a> for client-side code</li>
      <li><a href="https://picocss.com/">Pico CSS</a> for layout and styling</li>
      <li><a href="https://lucide.dev/">Lucide</a> for icons</li>
    </ul>
    <p>To help you get started:</p>
    <ul>
      <li><strong>Server-side rendering</strong>. Pages are pre-rendered on the server for fast first paint.</li>
      <li><strong>File-based routing</strong>. Each file in the <code>pages/</code> directory becomes a route automatically.</li>
      <li><strong>Nuxt dev server</strong>. In development mode, Nuxt runs with hot module replacement so the page updates instantly when you modify any file.</li>
    </ul>
    <p>The <code>ClientApp</code> subdirectory is a Nuxt application. Open a command prompt there to run <code>npm</code> commands such as <code>npm run start</code> or <code>npm install</code>.</p>
  </div>
</template>
